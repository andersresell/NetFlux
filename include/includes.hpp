#pragma once

#include <vector>
#include <string>
#include <iostream>
#include <map>
#include <eigen3/Eigen/Dense>

using string = std::string;
template <typename T> 
using vector = std::vector<T>;
using std::cout;
using std::endl;